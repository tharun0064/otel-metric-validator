# build output
bin/
.venv/
.env

# collector config holds live DB password + NR api-key — never commit
config.yml

# shared metrics dir (collector writes here); keep the dir, ignore contents
shared/*
!shared/.gitkeep
